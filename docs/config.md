# How to set up the config

## Format
    <key> = <value> [\n]

#### ! The file MUST end with a '\n' !

## Keys:
    ### Server-port: the port for the http server
    ### db-port: the port of the postgresql db

